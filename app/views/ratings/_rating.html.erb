<div class="rating">
    Title: <%= link_to_unless_current rating.title, rating %><br>
    Ratings: <%= rating.rating %><br>
    Description: <%= rating.description %>
    
    <%= month_calendar do |date| %>
  <%= date %>
<% end %>

<%= link_to "chracters", rating_characters_path(rating) %>
   
</div>